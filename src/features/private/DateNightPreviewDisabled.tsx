export function DateNightPreviewPage() {
  return (
    <section className="protected-route-notice">
      <div className="protected-route-icon">
        <span aria-hidden="true">!</span>
      </div>
      <p className="section-label">Private founder preview</p>
      <h1>This private preview is intentionally stripped from this build.</h1>
      <p>
        Use the local CityAtlas dev server when you need the founder preview route for rehearsals or
        screen-share prep.
      </p>
    </section>
  );
}
